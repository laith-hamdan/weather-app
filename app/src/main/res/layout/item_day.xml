<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="60dp"
    android:paddingStart="16dp"
    android:paddingEnd="16dp">

    <!-- Day name: start-anchored -->
    <TextView
        android:id="@+id/text_day_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textAppearance="?attr/textAppearanceLabelLarge"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/image_weather_icon"
        app:layout_constraintHorizontal_weight="1"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Monday" />

    <!-- Weather icon: centered in the row -->
    <LinearLayout
        android:id="@+id/icon_column"
        android:layout_width="wrap_content"
        android:layout_height="0dp"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/text_temp_range"
        app:layout_constraintStart_toEndOf="@id/text_day_label"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/image_weather_icon"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:contentDescription="@null"
            tools:src="@drawable/ic_weather_clear" />

        <TextView
            android:id="@+id/text_precip_badge"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="?attr/colorPrimary"
            android:textSize="10sp"
            android:visibility="gone"
            tools:text="3 mm"
            tools:visibility="visible" />
    </LinearLayout>

    <!-- Temperature range: end-anchored -->
    <TextView
        android:id="@+id/text_temp_range"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAppearance="?attr/textAppearanceBodyLarge"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="28° / 19°" />

</androidx.constraintlayout.widget.ConstraintLayout>
